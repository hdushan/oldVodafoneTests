- locals[:details].each do |key, value|
  -if value.class == Hash
    %p= "#{key}"
    .margin-wrapper=haml :trace_without_styling, :locals => { :details => value }
  -else
    %p= "#{key}: #{value}"


= haml :_shipping_info, locals: { shipping_details: locals[:shipping_details]}

= haml :_info_links