%h3.tracking-info-heading Shipping Details

%p TODO: display tracking event here
-#
  %p= "Status: #{locals['status']}"
  %table
    %tr
      %th Location
      %th Description
      %th Date/Time
      %th Signed by
    - locals['events'].each do |event|
      %tr
        %td= "#{event['location']}"
        %td= "#{event['description']}"
        %td= "#{event['date_time']}"
        %td= "#{event['signer']}"
