
%h2 Shipping details
-if locals['error']
  .alert
    .icon
      %img{src: 'images/alert_icon.png'}
    %span.msg= "There was an error while tracking your order: #{locals['error']}"
- else
  %p= "Status: #{locals['status']}"
  %table
    %tr
      %th Location
      %th Description
      %th Date/Time
      %th Signed by
    - locals['events'].each do |event|
      %tr
        %td= "#{event['location']}"
        %td= "#{event['description']}"
        %td= "#{event['date_time']}"
        %td= "#{event['signer']}"