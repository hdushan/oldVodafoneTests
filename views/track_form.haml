%article
  %h1 Trace your order.

  %form#track-form.form{ action: '/tnt', method: 'post' }
    %fieldset
      %label.tracking_label{:for => 'tracking_id'} Tracking number
      .input-wrapper
        %input#tracking_id{:type => 'text', :name => 'tracking_id', :autofocus => true, :value => @order_id}

      - if @error
        #server-error-msg.error-msg-block
          %img.glyph.warning{src: '../images/empty.png'}
          .error-msg
            =@error

      #input-validation-msg.error-msg-block.hidden
        %img.glyph.warning{src: '../images/empty.png'}
        .error-msg
          The Order Number you have entered is not in a valid format.
          Please check the number on your confirmation email and enter it as it appears on the email.

    .input-wrapper
      %button.trace.tnt-button#trace(type='submit')
        %span.strong
          Trace
        your order

  = haml :_loading_spinner

  =haml :_terms_link
